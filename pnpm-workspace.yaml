packages:
  - 'packages/*'

# These packages need their install scripts to run: esbuild unpacks a prebuilt
# binary, better-sqlite3 compiles a native module.
allowBuilds:
  better-sqlite3: true
  esbuild: true
